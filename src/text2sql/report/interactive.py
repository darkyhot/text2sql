"""Интерактивность HTML-отчётов (этап D, слой рендера).

Теперь на ВШИТОМ Tabulator (offline-inline, `render.py`): каждая таблица отчёта/расследования
становится интерактивной — сортировка по колонкам, фильтр-поиск в шапке, пагинация для длинных,
перетаскивание/resize колонок, экспорт CSV. Внешних запросов ноль (библиотека вшита в HTML).
Графики остаются встроенными PNG (matplotlib) — инвариант «HTML со встроенными картинками»
сохраняется; интерактивные ECharts-графики — в плейбук-поверхности (`surface.py`).

Применение — один пост-процессор `enhance(html)`; точки рендера таблиц трогать не нужно.
"""

from __future__ import annotations

from .render import enhance_tables


def enhance(html: str) -> str:
    """Вшить Tabulator и сделать все таблицы интерактивными. Идемпотентно."""
    return enhance_tables(html)
